<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-black text-white ">
    <div>
        <div class=" flex justify-end itmes-center mt-5 overflow-hidden >
            <a class="bg-red-600  p-1 tracking-tighter border-red-600 rounded " href="/logout">Logout</a>
        </div>
        <div class=" m-5 mt-3">
            <div class="flex itmes-start gap-3">
  <div class=" w-10 h-10  rounded-md">
   <img class="w-full h-full object-cover rounded-md"
     src="/images/uploads/<%= user.profilepic %>"
     alt="">

</div>
            <h2 class="capitalize">Hello,  <%=user.name%>👋 </h2>
            </div>
          
            <h4 class=" tracking-tighter text-zinc-400 mt-5"> you can create a new post</h4>
        </div>
        <form action="/post" method="post">
            <textarea placeholder=" What's on your mind ?"
                class=" bg-transparent border rounded outline-none border-zinc-800 px-2 py-4 text-white w-1/3 ml-5 resize-none overflow-hidden"
                name="content"></textarea>
            <button class=" bg-blue-500 text-white px-4 py-1 block mt-1 mx-5 rounded cursor-pointer">Create
                NewPost</button>

        </form>
    </div>

    <h3 class="text-zinc-300 mt-17 ml-5"> Your Posts.</h3>
    <div class="postcontainer ml-5 mt-5 flex gap-5 flex-wrap">

        <% user.posts.reverse().forEach(function(post){ %>

            <div class="border border-zinc-700 rounded p-5 bg-zinc-800 w-1/3">
                <div>
                    <h4 class="text-blue-500 mb-3">
                        <%=user.username%>
                    </h4>
                    <p class="tracking-tighter">
                        <%=post.content%>
                </div>
                <div class=" flex gap-7 mt-3">
                    <div class=" flex gap-1  ">
                        <small class=" text-yellow-500">
                            <%=post.likes.length %>
                        </small>
                        <a class="text-blue-500 text-[15px]" href="/like/<%=post.id%>">
                           <%= post.likes.indexOf(user._id)=== -1 ? "👍🏻": "👎🏻" %>
                        </a>
                    </div>

                    <a class="text-zinc-500 text-[15px]" href="/edit/<%=post.id%>">edit</a>
                </div>
            </div>

            <% })%>



    </div>

</body>

</html>